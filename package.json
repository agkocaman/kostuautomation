{
  "name": "hybs_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pw:homePageTR": "cross-env ENV=2 npx playwright test --project=chromeTR",
    "pw:homePageTR-ui": "cross-env ENV=2 npx playwright test --project=chromeTR --ui",
    "pw:homePageEN": "cross-env ENV=2 npx playwright test --project=chromeEN",
    "pw:homePageEn-ui": "cross-env ENV=2 npx playwright test --project=chromeEN --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.15.16",
    "cross-env": "^7.0.3",
    "tsconfig-paths": "^4.2.0"
  }
}